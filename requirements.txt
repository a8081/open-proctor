# Install the package in editable mode with all deps:
#   pip install -e .
#
# For GPU acceleration (CUDA), also install:
#   pip install "openproctor[gpu]"
# Or manually:  pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# For development (pytest, etc.):
#   pip install -e ".[dev]"

-e .
